[package]
name = "reputation"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk.workspace = true

# testutils butuh soroban-ledger-snapshot yang belum available di 27.0.2
# Tests akan di-enable ketika dependency tersedia

